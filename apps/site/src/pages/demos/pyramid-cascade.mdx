---
layout: ../../layouts/Demo.astro
title: Pyramid cascade (3-5-5-5-3)
subtitle: Jagged pyramid - ways pay - gravity refill (survivors stay, only gaps drop)
tags: [pyramid, cascade, ways, gravity]
description: A diamond-shaped 3-5-5-5-3 reel set built with visibleRowsPerReel + center anchor. Cascade refills are real gravity - survivors fall only as far as the cleared slots beneath them, symbols with no cleared slots below them don't move at all.
---

import PyramidCascadeDemo from '../../components/demos/PyramidCascadeDemo.tsx';

<PyramidCascadeDemo client:only="react" />

## The mechanic

```ts
new ReelSetBuilder()
  .reels(5)
  .visibleRowsPerReel([3, 5, 5, 5, 3])
  .reelAnchor('center')   // diamond shape
  .cascade(DropRecipes.stiffDrop)
```

`stiffDrop` instead of `cascadeDrop` because we want the symbols to land
hard - the bouncy default fights the win animation that arrives a frame
later.

## Gravity refill (not "redrop the whole frame")

The default cascade phase falls **all** old symbols out then drops **all**
new ones in. That's wrong for a tumble: survivors should stay put, only
the gaps left by winners should refill from above.

The fix is `tumbleToGrid()` from `examples/shared/cascadeLoop.ts`:

```ts
await vanish(winnerCells);                              // pop animation
const next = computeRefillGrid(grid, winnerCells);      // shift survivors + new top
await tumbleToGrid(reelSet, next, winnerCells, { dropDuration: 380 });
```

Per column it computes per-cell offsets:

- New symbol filling cleared row R -> starts (R+1) slots above the viewport.
- Survivor at final row R, originally at row N -> falls (R - N) slots.
- A symbol whose `R === N` doesn't move at all.

So a 5-row reel with a single winner at row 0 only animates one symbol -
the new top. Everything below stays put.

## Ways evaluation on a jagged grid

Same algorithm as MultiWays: walk reels left-to-right counting reels
that contain the kind (or wild). The pyramid's 3-row outer reels just
have fewer cells per reel - the chain logic doesn't care:

```ts
for (let c = 0; c < REEL_COUNT; c++) {
  const matches = grid[c].flatMap((s, r) =>
    s === kind || s === 'wild' ? [{ reelIndex: c, rowIndex: r }] : []);
  if (matches.length === 0) break;
  cellsByReel.push(matches);
}
```

## Round flow

1. `roundBus.emit('round:reset')` - WinBox to 0.
2. Initial `reelSet.spin()` -> `setDropOrder('ltr')` -> `setResult(grid)`.
3. Loop until no wins:
   - Evaluate ways, dedupe winning cells across overlapping wins.
   - `roundBus.emit('win:add', cascadeWin)` - WinBox additively tickups.
   - `vanish(cells)` - scale-from-center + alpha to 0.
   - `tumbleToGrid(...)` - gravity refill.
4. Status shows `N CASCADES - TOTAL`.
