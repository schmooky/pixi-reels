id: basics-008
group: basics
question: How do I show a static board with no spinning at all?
answer: Build with `.initialFrame(grid)` and simply never call `spin()` — the
  board renders the seeded symbols statically.
links: []
