id: integ-007
group: integ
question: How do I sequence multi-step server features (round → round → collect)?
answer: Drive each step with an `await` (e.g. `board.respin(round.hits)` per
  round) against the server's per-step response.
links:
  - label: "Recipe: Hold & Win respin"
    href: /recipes/hold-and-win/
