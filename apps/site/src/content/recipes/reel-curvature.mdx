---
title: "Reel curvature"
group: rendering
oneLiner: "Project the reels onto a drum with one number - per set or per reel - so cells keystone into real trapezoids instead of flat rectangles."
order: 65
tags:
  - curve
  - curvature
  - perspective
  - projection
  - cylinder
  - drum
  - 3d
apis: []
steps: []
---

## One number

A slot reel is a drum. The middle cell faces you; the top and bottom ones have rotated away, so their far edge sits further from your eye. A camera turns those into **trapezoids**, not smaller rectangles.

`.curve(amount)`. `0` flat (default, free), `1` hard barrel. Most boards want `0.3`&ndash;`0.5`.

Grid lines on the tiles are the tell: they converge on the outer rows, stay parallel on the middle one.

<RecipeDemo code="curved-reels" height={420} />

`depth` is perspective strength - how much smaller an edge cell is than the middle one. `0` = orthographic: cells still bunch, nothing recedes, nothing keystones. Defaults to `amount * 0.5`.

```ts
builder.curve(0.45);                          // depth 0.225
builder.curve({ amount: 0.45, depth: 0.35 }); // stronger
```

Capped just under `cos(arc)`. Past that the projection stops being monotonic and edge cells fold back over each other, so a deep curve saturates instead of turning inside out.

<a id="curved-reels-focus"></a>

## Where the camera stands

Default: one camera per reel, so every reel is its own drum. `.curveFocus('set')`: one camera on the middle of the board, so receding cells also lean IN and the grid reads as one wide cylinder.

```ts
builder.curve(0.5).curveFocus('reel');     // default - five separate drums
builder.curve(0.5).curveFocus('set-lean'); // halfway
builder.curve(0.5).curveFocus('set');      // one camera, whole board
```

Watch the outer reels tilt toward the middle while the centre one barely moves. `'set-lean'` is usually the sweet spot on a 5-wide board.

<RecipeDemo code="curved-reels-focus" height={420} />

**Gotcha:** leaning cells cross their own column, and the per-reel `RectMaskStrategy` would slice the overhang off. Anything but `'reel'` auto-selects `SharedRectMaskStrategy`. An explicit `.maskStrategy(...)` still wins.

<a id="curved-reels-per-reel"></a>

## One drum, not five

`.curvePerReel([...])` - a camera each. Bend the middle reels hardest, ease off outward, and the board reads as one cylinder. Length must equal `reels()`; entries take the same shorthand or object form as `.curve()`.

<RecipeDemo code="curved-reels-per-reel" height={420} />

## Tuning it live

`reelSet.setCurve()` re-projects at runtime - same value or a per-reel array. Dial it against your art instead of rebuilding:

```ts
reelSet.setCurve(0.4);
reelSet.setCurve([0.2, 0.35, 0.5, 0.35, 0.2]);
reelSet.setCurve(0); // flat
```

## What draws the trapezoid

A `Container` transform is affine. It scales, skews, rotates - it can never make a rectangle a trapezoid. Anything built on `scale` alone gives you a smaller rectangle and calls it perspective.

So the engine hands each symbol a **projected quad** via `ReelSymbol.applyCellQuad()`:

- **`SpriteSymbol` / `AnimatedSpriteSymbol`** map it onto a PixiJS `PerspectiveMesh` - true keystone, **no extra render pass**, mesh allocated only once a reel is curved. Requires a texture that owns its whole source.
- **Everything else** gets a *uniform* scale about the quad's centre, sized to fit INSIDE it so no cell overlaps its neighbour. Uniform on purpose: squashing one axis turns a `7` into a squashed `7`, not a `7` at an angle. Spine and composite subtrees land here - projecting them would mean a render texture every frame.

Own symbol, single texture? Write `applyCellQuad()`. `PerspectiveCell` is exported and does the mesh plumbing.

**Known limitation:** an *atlas sub-frame* does not take the mesh path yet. The mesh addresses its source with 0..1 UVs and remapping them onto the frame has not produced a correct draw here, so `canProjectTexture()` refuses those and the symbol falls back to the affine fit - right, but not keystoned. That is most production art today. Whole-source textures are unaffected.

<a id="curved-reels-gems"></a>

## On real art

Grid tiles are a measuring instrument. This is the same curve on shipped symbols - Playson's Supercharged Diamonds 3, the sheet the sprite Hold &amp; Win recipes use - at `curveFocus('set-lean')`.

Atlas sub-frames, so per above: affine fit, no keystone. Cells recede and lean, gems never distort. They are also letterboxed and heavily trimmed, so `BlurSpriteSymbol` reports where its art really sits via `cellInset` - without it the drum would project the empty cell instead of the gem.

Blur-on-spin is unaffected: motion-blur frames on `onReelSpinStart`, back on landing.

<RecipeDemo code="curved-reels-gems" height={420} />

## Art that does not fill its cell

Most does not - a trimmed frame is a small shape in a big transparent box. Project the whole cell and you inflate the symbol and hand it the cell's keystone instead of its own.

Symbols report their real rect via `ReelSymbol.cellInset`. Sprite symbols derive it from the texture's trim, so a TexturePacker atlas needs no help. Override it if your symbol places art some other way.

## The engine does not notice

The strip still marches on flat coordinates. The projection is a view-local quad and never touches a view's `position`, so `setResult()`, wrapping, cascades, big symbols and MultiWays reshapes behave exactly as flat.

`getCellBounds()` returns the projected cell's **bounding box**; `getCellQuad()` returns the four corners it is actually drawn on. Outline with the quad, hit-test with the box.

**Gotcha:** the affine fallback owns `view.scale`. A win animation tweening `symbol.view.scale` to an absolute value overwrites the projection while it runs. Sprite symbols dodge this by pulsing the mesh instead; in your own, animate a child of `view`.

**Gotcha:** curvature is presentation only. It never changes what lands where, what `getVisibleGrid()` returns, or any event payload.
